
/**
 * Replace all spaces in a string with no spaces.
 * @param {string} s - string - the string to be manipulated
 * @returns The string with all spaces removed.
 */
 export const noSpace = (s: string): string => {
  return s.replace(/\s/g, '');
};

